<ui:FluentWindow
    x:Class="ComfyUIRunWorkflow.Views.Windows.ImagePreviewWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:helpers="clr-namespace:ComfyUIRunWorkflow.Helpers"
    Title="{Binding Source={x:Static helpers:LocalizationManager.Instance}, Path=[ImagePreview_Title]}"
    Width="900"
    Height="700"
    MinWidth="320"
    MinHeight="240"
    WindowStartupLocation="CenterOwner"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="{Binding Source={x:Static helpers:LocalizationManager.Instance}, Path=[ImagePreview_Title]}"
            Height="36"
            CloseWindowByDoubleClickOnIcon="False" />

        <Border
            Grid.Row="1"
            Margin="16"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4">
            <Image
                x:Name="PreviewImage"
                Margin="8"
                Stretch="Uniform" />
        </Border>
    </Grid>
</ui:FluentWindow>
